using Alternet.UI;
using System;

namespace $rootnamespace$
{
    internal class Program
    {
        [STAThread]
        public static void Main(string[] args)
        {
            var application = new Application();
            var window = new $safeitemrootname$();

            application.Run(window);

            window.Dispose();
            application.Dispose();
        }
    }
}